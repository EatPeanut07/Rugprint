const base=(process.env.RUGPRINT_BASE_URL||"").replace(/\/$/,"");const secret=process.env.RUGPRINT_ADMIN_SECRET;const interval=Math.max(Number(process.env.RUGPRINT_BACKFILL_INTERVAL_MS||30000),10000);if(!base)throw new Error("RUGPRINT_BASE_URL is missing");if(!secret)throw new Error("RUGPRINT_ADMIN_SECRET is missing");const sleep=ms=>new Promise(r=>setTimeout(r,ms));async function run(){const c=new AbortController(),timeout=setTimeout(()=>c.abort(),120000);try{const r=await fetch(base+"/api/intelligence/backfill",{method:"POST",headers:{Authorization:"Bearer "+secret,"Content-Type":"application/json"},body:JSON.stringify({batch:5,pages:3}),signal:c.signal});const t=await r.text();if(!r.ok)throw new Error("Backfill API "+r.status+": "+t.slice(0,500));console.log("RugPrint backfill",t)}finally{clearTimeout(timeout)}}console.log("RugPrint historical intelligence worker started");while(true){try{await run()}catch(e){console.error("RugPrint worker",e)}await sleep(interval)}
