const endpoint=process.env.SUPABASE_URL?.replace(/\/$/,""),key=process.env.SUPABASE_SECRET_KEY||process.env.SUPABASE_SERVICE_ROLE_KEY;const treasury=process.env.RUGPRINT_TREASURY_WALLET?.trim(),price=Number(process.env.RUGPRINT_PRO_PRICE_SOL||"0.5");async function db(path:string,init?:RequestInit){if(!endpoint||!key)throw new Error("Database not configured.");const r=await fetch(`${endpoint}/rest/v1/${path}`,{...init,headers:{apikey:key,Authorization:`Bearer ${key}`,"Content-Type":"application/json",Prefer:"return=representation",...(init?.headers||{})},cache:"no-store"});if(!r.ok)throw new Error(`Database error (${r.status}): ${await r.text()}`);const t=await r.text();return t?JSON.parse(t):null}export async function proStatus(userKey:string){const s=await db(`rugprint_alert_subscriptions?user_key=eq.${encodeURIComponent(userKey)}&select=id,plan`);const p=await db(`rugprint_pro_payments?user_key=eq.${encodeURIComponent(userKey)}&select=*&order=created_at.desc&limit=1`);return{plan:s?.[0]?.plan||"free",payment:p?.[0]||null,priceSol:price,treasuryWallet:treasury||null}}export async function submitPayment(userKey:string,signature:string,payerWallet:string){if(!treasury)throw new Error("Treasury wallet is not configured.");if(!/^[1-9A-HJ-NP-Za-km-z]{32,44}$/.test(payerWallet)||!/^[1-9A-HJ-NP-Za-km-z]{64,100}$/.test(signature))throw new Error("Invalid wallet or transaction signature.");const rows=await db("rugprint_pro_payments?on_conflict=signature",{method:"POST",headers:{Prefer:"resolution=ignore-duplicates,return=representation"},body:JSON.stringify({user_key:userKey,signature,payer_wallet:payerWallet,treasury_wallet:treasury,expected_sol:price,status:"pending"})});return rows?.[0]||{signature,status:"pending"}}