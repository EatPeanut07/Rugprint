import{NextResponse}from"next/server";import{discoverSeeds}from"@/lib/intelligence-engine";export const runtime="nodejs";const auth=(r:Request)=>{const s=process.env.RUGPRINT_ADMIN_SECRET?.trim();return Boolean(s)&&r.headers.get("authorization")===`Bearer ${s}`};export async function POST(req:Request){if(!auth(req))return NextResponse.json({ok:false},{status:401});try{return NextResponse.json({ok:true,...await discoverSeeds()})}catch(e){return NextResponse.json({ok:false,error:e instanceof Error?e.message:"failed"},{status:400})}}
