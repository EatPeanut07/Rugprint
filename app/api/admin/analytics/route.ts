import{NextResponse}from"next/server";import{analyticsSummary}from"@/lib/analytics";export const runtime="nodejs";export async function GET(req:Request){const secret=req.headers.get("x-rugprint-admin")||new URL(req.url).searchParams.get("key");if(!process.env.RUGPRINT_ADMIN_SECRET||secret!==process.env.RUGPRINT_ADMIN_SECRET)return NextResponse.json({error:"Unauthorized"},{status:401});try{return NextResponse.json(await analyticsSummary())}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Analytics failed"},{status:500})}}