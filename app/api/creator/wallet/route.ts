import{NextResponse}from"next/server";import{inspectCreatorWallet}from"@/lib/creator-intelligence";export const runtime="nodejs";export const maxDuration=60;export async function POST(req:Request){try{const b=await req.json(),wallet=String(b?.wallet||"").trim();if(!wallet)return NextResponse.json({error:"Enter a creator wallet."},{status:400});return NextResponse.json(await inspectCreatorWallet(wallet));}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Creator lookup failed"},{status:500});}}