import{NextResponse}from"next/server";import{createTelegramLink}from"@/lib/telegram-link";export const runtime="nodejs";export async function POST(req:Request){try{const{userKey}=await req.json();if(!userKey)return NextResponse.json({ok:false,error:"Missing user key."},{status:400});const token=await createTelegramLink(String(userKey));const bot=process.env.TELEGRAM_BOT_USERNAME?.replace(/^@/,"");if(!bot)return NextResponse.json({ok:false,error:"Telegram bot username is not configured."},{status:500});return NextResponse.json({ok:true,url:`https://t.me/${bot}?start=${token}`})}catch(e){return NextResponse.json({ok:false,error:e instanceof Error?e.message:"failed"},{status:500})}}