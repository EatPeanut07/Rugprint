"use client";import{useState}from"react";export default function Report(){const[type,setType]=useState("x"),[value,setValue]=useState(""),[why,setWhy]=useState(""),[links,setLinks]=useState(""),[msg,setMsg]=useState<any>(null),[busy,setBusy]=useState(false);async function submit(){setBusy(true);setMsg(null);let k=localStorage.getItem("rugprint_reporter_key");if(!k){k=crypto.randomUUID();localStorage.setItem("rugprint_reporter_key",k)}const r=await fetch("/api/reports",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({subjectType:type,subjectValue:value,explanation:why,evidenceLinks:links.split("\n").map(x=>x.trim()).filter(Boolean),reporterKey:k})});setMsg(await r.json());setBusy(false)}return <main><header className="topbar"><a className="brand" href="/">⌁ RUGPRINT</a><div className="status"><i/> COMMUNITY EVIDENCE</div><a className="github" href="/creator">Creators ↗</a></header><section className="creatorhero"><div className="kicker">SUBMIT EVIDENCE</div><h1>Bring receipts.</h1><p>Community reports are leads, not verdicts. RugPrint keeps community allegations separate from independently verified evidence.</p><div className="reportform"><label>Who or what are you reporting?<select value={type} onChange={e=>setType(e.target.value)}><option value="x">X username</option><option value="pumpfun">Pump.fun username</option><option value="fomo">Fomo username</option><option value="wallet">Wallet</option><option value="contract">Contract</option></select></label><label>Username / wallet / contract<input value={value} onChange={e=>setValue(e.target.value)} placeholder={type==="x"?"@username":"Enter value"}/></label><label>What happened?<textarea value={why} onChange={e=>setWhy(e.target.value)} placeholder="Explain the suspected creator activity and how the evidence supports it."/></label><label>Evidence links, one per line<textarea value={links} onChange={e=>setLinks(e.target.value)} placeholder={"Solscan transaction / token contract / archived post / other evidence URL"}/></label><button disabled={busy||!value.trim()||why.trim().length<30||!links.trim()} onClick={submit}>{busy?"Submitting…":"Submit evidence"}</button></div>{msg?.ok&&<div className="successbox">Evidence submitted. Status: <b>UNVERIFIED / SUBMITTED</b>. It will not be treated as a RugPrint finding until supported by evidence.</div>}{msg?.error&&<div className="error">{msg.error}</div>}</section></main>}