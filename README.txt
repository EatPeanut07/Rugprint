RugPrint Auto Monitor Bundle

1. Run AUTO-MONITOR-SCHEMA.sql in Supabase first.
2. Upload every other file to the matching path in GitHub, replacing existing files where applicable.
3. Railway should deploy automatically.
4. After SUCCESS, send /start in Telegram once.

Included:
- Automatic 5-minute wallet/contract watch monitoring on the running Railway instance.
- Baseline-first deduplication, so adding a watch does not alert on old history.
- Helius-based new activity detection.
- Trial quota remains 3 successfully delivered alerts/month.
- Cleaner Telegram inline buttons, hiding Railway URLs behind RugPrint buttons.
- Creator metric: observed token scans after the cluster's first recorded adverse evidence.
- Username watches are NOT falsely presented as live monitoring. Pump.fun/Fomo/X need a verified identity-to-wallet link before automatic on-chain monitoring.

Important:
- New transfer/swap/launch activity is described as activity, not automatically as fraud.
- The first monitor pass establishes a baseline and sends no alert.
- Payment activation is not included in this bundle.
